{
  "name": "workbench",
  "version": "0.1.0",
  "private": true,
  "description": "Electron and Three.js renderer for the laboratory workbench.",
  "main": "electron/main.js",
  "scripts": {
    "build": "vite build",
    "start": "npm run build && electron .",
    "preview": "npm run build && electron .",
    "offscreen": "npm run build && electron . --offscreen"
  },
  "dependencies": {
    "three": "latest"
  },
  "devDependencies": {
    "electron": "latest",
    "vite": "latest"
  }
}